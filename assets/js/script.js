// get from local storage
// check local storage for data related to time blcok
// getitem forw aht youd thing its called
// if item found then put in input box




// grab the buttons -- addEventListeners
$('.saveButton').on('click', function() {
    // set of instuctions
    console.dir($(this).attr('id'));
    console.dir(this.id);
    // When a user clicks a button
    // -- get the input from the input box
    // -- store in local storage
} )
